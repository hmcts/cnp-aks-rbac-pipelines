---

gateways:
  - gateway_configuration:
      backend_pool_addresses:
        - 10.10.19.250
        - 10.10.23.250
      host_name_suffix: service.core-compute-aat.internal
    app_configuration:
    # RPE
      - product: draft-store
        component: service
      - product: cmc
        component: pdf-service
    # Probate
      - product: probate
        component: probate-business-service
    # I&A

    # Reference DATA

    # Divorce
