---

gateways:
  - gateway_configuration:
      backend_pool_addresses:
        - 10.10.1.250
        - 10.10.3.250
      host_name_suffix: service.core-compute-sandbox.internal
    app_configuration:
    # RPE
      - product: draft-store
        component: service
      - product: cmc
        component: pdf-service
    # Probate

    # I&A

    # Reference DATA

    # Divorce
    - product: evidence-management-client-api
      component: service
